# 実装確認記録

2026-09-25 実施。

- Next.js の本番ビルド、型チェック、静的書き出しが成功。
- `git diff --check` 成功。
- 出力された `out/` をローカルHTTPサーバーで実際に表示して確認。
- 1440pxのデスクトップ、1024px・768pxのタブレット、390px・320pxのスマートフォン幅で確認。横方向のはみ出しなし。
- 全てのページ内リンクに遷移先あり。確認時の画像読み込みエラーなし。
- スマートフォンメニューの開閉、セクションへの移動、スクロールロック解除を確認。
- FAQの開閉と、同一グループの別回答への切り替えを確認。
- 施工写真の拡大、施工前後の切り替え、Escapeで閉じる動作、元のボタンへのフォーカス復帰を確認。
- 電話・メール・LINE・Instagramのリンク先を既存情報と照合。発信やメッセージ送信は実施していない。
- 本番プレビューのブラウザーコンソールにエラー・警告なし。

公開サーバーへのアップロードは実施していない。静的出力設定と既存の所在地・代表者修正を保持。

## 2026-09-26 空き家管理ページ追加後

- 本番ビルド・型検査・2ページの静的書き出し成功。`out/akiya/index.html` を生成。
- 2ページのHTMLを解析し、画像・スクリプト・スタイル・内部リンクの参照先がすべて存在することを確認。各ページのh1は1つ。
- 実際の静的ファイルをローカルサーバーで表示。ヘッダーから林業→空き家管理、空き家管理→林業の両方向に遷移し、タイトルと現在の事業表示が切り替わることを確認。
- 新ページを1440px・1280px・1024px・390px・320pxで確認。横方向のはみ出しなし。320pxでも見出し・表のセルの文字の欠けなし。
- スマートフォンメニューを開き、追加サービスへのリンクで移動後にメニューが閉じ、スクロールロックが解除されることを確認。
- FAQの回答を切り替え、1項目だけが開く動作を確認。
- 新ページの画像読み込み失敗なし。コンソールの警告・エラーなし。
- 共通ヘッダーの問い合わせアイコンに読み上げ用の名前を追加。
- 新ページのcanonicalは `https://yasupachiringyo.jp/akiya/`。サイトマップ・robots.txtを静的出力に含めた。
- オプション金額は提供文と照合。月額・税込税別の確認は未完了。月額は個別見積り表記。
- 外部への電話・メール・LINE送信、本番サーバーへの公開は実施していない。
